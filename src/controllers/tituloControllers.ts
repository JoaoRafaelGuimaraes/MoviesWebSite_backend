import * as filmeControllers from './filmeControllers';
import * as seriesControllers from './serieControllers';

const tituloControllers = {
    filmeControllers,
    seriesControllers
};

export default tituloControllers;
